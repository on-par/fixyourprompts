/**
 * LazyWrapper component exports
 * Provides clean import interface for the LazyWrapper utilities
 */

export { 
  LazyWrapper, 
  createLazyComponent, 
  withLazyWrapper,
  default 
} from './LazyWrapper';
export type { 
  LazyWrapperProps, 
  LazyWrapperOptions 
} from './LazyWrapper';