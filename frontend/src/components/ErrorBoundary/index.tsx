/**
 * ErrorBoundary Component Export
 * 
 * Centralized export for the ErrorBoundary component
 */

export { ErrorBoundary, default } from './ErrorBoundary';
export type { ErrorBoundaryProps } from '../../types/components';